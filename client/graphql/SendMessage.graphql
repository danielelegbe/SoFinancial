mutation SendMessage($sendMessageData: NewMessageInput!) {
  sendMessage(data: $sendMessageData) {
    id
    content
    createdAt
    from {
      id
      username
      avatar
    }
    to {
      id
      username
      avatar
    }
  }
}
